# Movies List SOAP App
Node.js Movies List App with SOAP Service

## 📌 Description
This project is a Node.js web application built using Express and MongoDB.  
It allows users to register, log in, and view a list of movies.  
The application integrates a SOAP web service to retrieve and process movie-related data.

This project follows the MVC (Model–View–Controller) architecture.

## 🛠 Technologies Used
- Node.js
- Express.js
- MongoDB (Mongoose)
- EJS (Embedded JavaScript Templates)
- Passport.js (Authentication)
- bcrypt (Password hashing)
- SOAP Web Services
- Express Session

---

## ✨ Features
- User registration and login
- Secure password authentication
- Session management
- Movie listing page
- SOAP service integration
- MVC project structure


## ⚙️ Installation & Running the Project

1. Install **Node.js** and **MongoDB**
2. Clone or download the repository
3. Open the project folder in a terminal
4. Run the following commands:

```bash
npm install
node app.js

Open your browser and go to:
http://localhost:3000




